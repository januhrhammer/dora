# Email Configuration for Reminders using Mailjet
# Copy this file to .env and fill in your actual values

# Mailjet API Credentials
# Get these from: https://app.mailjet.com/account/apikeys
MAILJET_API_KEY=your_mailjet_api_key_here
MAILJET_API_SECRET=your_mailjet_api_secret_here

# Sender Information
# The FROM_EMAIL must be verified in Mailjet
# Go to: Mailjet Account Settings > Sender Addresses
FROM_EMAIL=your-verified-sender@example.com
FROM_NAME=Medicine Tracker

# Recipient Information
TO_EMAIL=recipient@example.com
TO_NAME=Grandma

# Authentication Configuration
# Username for the single user (no signup available)
AUTH_USERNAME=admin

# Password hash for authentication
# To generate a hash, run: python -c "from passlib.context import CryptContext; print(CryptContext(schemes=['bcrypt']).hash('your_password_here'))"
# Default password: "changeme" (CHANGE THIS!)
AUTH_PASSWORD_HASH=$2b$12$LQv3c1yqBWVHxkd0LHAkCOYz6TtxMQJqhN8/LewY5NU9cVr2/3P2W

# Secret key for JWT tokens (generate a random secret!)
# Generate with: python -c "import secrets; print(secrets.token_urlsafe(32))"
AUTH_SECRET_KEY=your-secret-key-change-this-in-production

# How to set up Mailjet:
# 1. Sign up for free at https://www.mailjet.com/
# 2. Verify your sender email address
# 3. Get your API keys from https://app.mailjet.com/account/apikeys
# 4. Copy them to the MAILJET_API_KEY and MAILJET_API_SECRET above
